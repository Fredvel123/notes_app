// EndPoints from REST API

const mainUrl = 'http://localhost:8000/'; // this is the url to my server inside my computer

export const getUsers = mainUrl + 'api/users/all';
export const signUpUrl = mainUrl + 'api/auth/signup';
